{
  "name": "service-portal",
  "private": true,
  "version": "0.0.0",
  "description": "Zero-dependency Docker service portal",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
